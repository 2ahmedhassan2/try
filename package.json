{
  "name": "educational-site", // Replace with your project name
  "version": "1.0.0",
  "private": true,
  "description": "An educational website built with React",
  "main": "src/index.js",
  "scripts": {
    "start": "react-scripts start", // Start development server
    "build": "react-scripts build", // Build for production
    "test": "react-scripts test",   // Run tests
    "eject": "react-scripts eject"  // Eject config (use cautiously)
  },
  "dependencies": {
    "react": "^18.2.0", // React library
    "react-dom": "^18.2.0", // React DOM library
    "react-scripts": "5.0.1" // React Scripts (config and tooling)
  },
  "devDependencies": {
    // Add dev-only dependencies here if needed
  },
  "homepage": ".", // Required for relative paths when deploying to Netlify
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
